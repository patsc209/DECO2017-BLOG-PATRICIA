{
  "type": "module",
  "scripts": {
    "build": "node scripts/build.js",
    "preview": "npm run build && npx --yes serve dist -l 3000"
  },
  "dependencies": {
    "chalk": "^5.3.0",
    "date-fns": "^4.1.0",
    "gray-matter": "^4.0.3",
    "marked": "^17.0.2"
  }
}
